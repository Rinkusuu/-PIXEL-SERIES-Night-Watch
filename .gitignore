node_modules/
dist/
.DS_Store
*.local

# Screenshot and browser-automation scratch. These kept landing in commits
# because they are written into the project root while inspecting the app.
.playwright-mcp/
/*.png
